import Root, {
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
	buttonVariants,
} from "./hotkeybutton.svelte";

export {
	Root,
	type ButtonProps as Props,
	//
	Root as HotkeyButton,
	buttonVariants,
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
};
